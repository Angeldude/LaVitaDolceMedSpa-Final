<% @products.select do |item| %>
  <% if item.collection == params[:collection] %>
    <p><%= link_to item.name, controller: "products", action: "show", id: item %></p>
  <% end %>
<% end %>
<%= link_to 'Back', products_path %>